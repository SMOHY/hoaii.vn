## .NET
bin/
obj/
*.user
.vs/

## Editor
.vscode/

## Node (if a build tool is added later)
node_modules/

# User-uploaded media (admin) — not source
src/Hoaii.Web/wwwroot/uploads/

# Build/publish output — contains appsettings.Production.json with the VPS DB password
publish_out/

# Local run logs from run-migration.ps1 / start.ps1
run-std*.log

# Ảnh chụp màn hình dùng để tự kiểm và để dựng file báo cáo gửi khách — sinh lại được
# bằng tests/e2e/, không phải mã nguồn (53MB).
anh-kiem-tra/
anh-bao-cao/
